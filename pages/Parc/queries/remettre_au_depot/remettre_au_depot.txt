-- Fin de maintenance ou contrôle de retour réussi : le matériel est disponible.
UPDATE materiels
SET statut = 'au_depot'
WHERE id = {{ Table_Parc.selectedRow.id }}
  AND statut IN ('maintenance', 'a_controler');
