-- Le matériel reste sur l'ordre : il fait partie de la demande.
UPDATE ordres_transport
SET
    camion_id = NULL,
    tournee_id = NULL,
    ordre_passage = NULL,
    heure_prevue = NULL,
    statut = 'a_planifier'
WHERE id = {{ Table_Planifies.selectedRow.ordre_id }}
  AND statut = 'planifie';
