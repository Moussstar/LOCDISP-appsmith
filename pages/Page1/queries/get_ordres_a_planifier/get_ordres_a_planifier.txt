SELECT 
    ot.id AS ordre_id,
    c.nom_entreprise AS client,
    ch.nom_chantier AS chantier,
    ot.type_action,
    ot.statut
FROM ordres_transport ot
LEFT JOIN chantiers ch ON ot.chantier_id = ch.id
LEFT JOIN clients c ON ch.client_id = c.id
WHERE ot.statut = 'a_planifier'
ORDER BY ot.id ASC;