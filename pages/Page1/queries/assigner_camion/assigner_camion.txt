-- Le matériel est déjà porté par l'ordre : on n'affecte que le camion.
UPDATE ordres_transport
SET
    camion_id = {{ Select_Camion.selectedOptionValue }}::integer,
    statut = 'planifie'
WHERE id = {{ Table_Ordres.selectedRow.ordre_id }}
  AND statut = 'a_planifier';
