SELECT
    id,
    immatriculation,
    modele,
    charge_max_kg,
    longueur_utile_cm,
    largeur_utile_cm,
    hauteur_utile_cm,
    modele || ' (' || immatriculation || ') - ' || charge_max_kg || ' kg' AS label_affiche
FROM camions
WHERE actif
ORDER BY modele;
